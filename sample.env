# Copy this file to .env and edit the values
# Create a secret key using Python Interactive Shell
# 1. Import get_random_secret_key() from django.core.management.utils.
# 2. Generate the Secret Key in the Terminal using the get_random_secret_key() function
# 3. Copy and Paste the Key into your SECRET_KEY variable in the settings.py
SECRET_KEY=secret-key-value
# Set DEBUG to True for development, False for actual use
DEBUG=False
# ALLOWED_HOSTS is a comma-separated list of hosts that can access the app.
# You can use wildcard chars (*) and IP addresses. Use * for any host.
ALLOWED_HOSTS='*.ku.th, localhost, 127.0.0.1, ::1, testserver'
# Your timezone
TIME_ZONE=Asia/Bangkok